name: bnk_kit
description: Transport for bnkscope Field. Everything reaches a cluster through one door, the apiserver.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  logging: ^1.2.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  bnkfield:
