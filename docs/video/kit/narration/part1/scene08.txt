A second kubeconfig, and a second cluster. This one runs NICo, so a NICo screen appears alongside the others. Control plane health, and the admin certificate with three hundred and four days left, which is the sort of thing worth knowing before it stops working. And a tenant control plane, whose endpoint is the cluster imported at the start. These two clusters are one system. Until now, nothing in the app said so.
