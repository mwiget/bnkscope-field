The same door gives a shell. This is pods exec, over a websocket, running the diagnostics that only exist inside the debug container. Interface counters first, the physical ports on the DPU. Then connection counts, read straight out of the tmstat tables. There is no terminal emulation here, and that is deliberate. This is for tools that print and exit, not a shell pretending it can run an editor.
