It starts empty. A kubeconfig comes in from Files, and Field reads every context in it, probes each one, and says what it found. Kubernetes one thirty-four, both nodes ready, and the roles it detected from pod labels rather than namespace names. This cluster runs BNK, and its workloads are wired through the DPU service API. It needs exactly one connection to do any of this: the API server.
