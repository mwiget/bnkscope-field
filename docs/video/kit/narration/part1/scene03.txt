Overview asks one question. Is anything wrong, and where. Clusters sort by trouble rather than by name. Two pods here are running but not ready, with twenty-eight and a hundred and seventy-six restarts. What it deliberately does not rank on is that restart count: on this lab an Argo pod has restarted thirty-nine times and is perfectly healthy. Readiness is the signal. Tapping a finding opens the pod, and the pod says why. Redis, on local host, connection refused.
